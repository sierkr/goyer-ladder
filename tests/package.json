{
  "name": "goyer-ladder-tests",
  "version": "5.4.3",
  "private": true,
  "description": "Testgereedschap voor de Goyer Golf MP Ladder. Alleen nodig voor de emulator- en browsertests; de rekentests draaien zonder dit alles.",
  "scripts": {
    "regels": "firebase emulators:exec --project demo-goyer --only firestore \"node tests/emulator/rules.test.cjs\"",
    "functies": "firebase emulators:exec --project demo-goyer --only firestore,auth,functions \"node tests/emulator/functions.test.cjs\"",
    "browser": "firebase emulators:exec --project demo-goyer --only firestore,auth,functions \"node tests/e2e/seed.cjs && playwright test --config=playwright.config.cjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "@playwright/test": "^1.49.0",
    "firebase-admin": "^12.0.0",
    "firebase-tools": "^13.0.0"
  }
}
